node_modules/
.DS_Store
*.log

# Built assets are generated from src/ and tools/generate-art.mjs.
# They are committed on purpose: `shopify theme push` uploads whatever is in
# theme/assets, so the built bundle must exist in the repo for a clean deploy
# from a fresh clone. Regenerate with `npm run build`.
